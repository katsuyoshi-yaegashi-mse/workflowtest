name: Manual Trigger

on: [ workflow_dispatch ]

jobs:
  ex1:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - run: |
        echo ====pwd run====
        pwd
        echo ====whoami run====
        whoami
        echo ====printenv run====
        printenv
        
